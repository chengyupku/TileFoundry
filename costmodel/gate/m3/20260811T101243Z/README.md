# TileFoundry Cost Model M3 Gate Record

Run ID: `20260811T101243Z`

Status: **PASS - M3 CLOSED**

This gate covers only M3 profile storage, exact resolution, local CUDA JIT,
the minimal real B200 copy provider, and snapshot/profile CLI workflows. M0,
M1, and M2 evidence and wheels remain unchanged. Public `build()` and `solve()`
remain M4 placeholders; M4 and M5 were not started.

## Golden Reference

The source of truth is `docs/spec/cost-model.md` sections 8.2, 8.3, and 8.4,
plus the M3 milestone in `docs/plans/b200-cost-model.md`. The retained real
workflow exercises exact typed keys, one measurement carrying distinct latency
and initiation-interval aggregates, setup outside CUDA event timing, atomic
publication, correctness validation, frozen replay, and a cache hit that does
not execute CUDA.

## Final Artifact

Wheel:

`wheel/tilefoundry_costmodel-2.0.0-py3-none-any.whl`

SHA256:

`75230665638482ce8cffadda9f392c10d48615733feef894dc5ec08e53a984a2`

`artifact-verification.log` verifies all 49 packaged Python/CUDA source members
byte-for-byte against the source tree, seven JSON schemas, B200 calibration,
base `dependencies=[]`, and the `cuda` and `test` extras. The wheel contains
both packaged CUDA sources.

## Gate Matrix

| Gate | Result | Evidence |
|---|---|---|
| Ruff check and format | PASS | `ruff-check.log`, `ruff-format.log` |
| Strict mypy | PASS, 47 source files | `mypy-strict.log` |
| Forward-reference lint | PASS | `forward-reference-lint.log` |
| macOS source tests | PASS, 83 passed / 3 real-B200 skips, 70% branch coverage | `source-tests.log` |
| Python 3.11 installed wheel base | PASS | `py311-base.log` |
| Python 3.11 installed wheel `[test]` | PASS, 77 passed / 9 optional-backend skips | `py311-test.log` |
| Python 3.12 installed wheel base | PASS | `py312-base.log` |
| Python 3.12 installed wheel `[test]` | PASS, 77 passed / 9 optional-backend skips | `py312-test.log` |
| Schema and calibration validation | PASS | `schema-validation.log`, `artifact-verification.log` |
| Source/wheel parity and package data | PASS | `artifact-verification.log`, `wheel-contents.txt` |
| node14 final wheel `[cuda,test]` | PASS, 80 passed / 6 CP-SAT skips, 71% branch coverage | `node14-wheel-py312.log` |
| node14 real B200 workflow | PASS, 6/6 | `node14-real-workflow.log` |
| M0/M1/M2 frozen manifests | UNCHANGED | `frozen-boundary.log`, `m2-closure-checksum.log` |
| M4/M5 boundary | PASS | `scope-boundary.log` |

node14 exposed eight NVIDIA B200 devices at compute capability 10.0 with driver
590.48.01. The final wheel selected `cuda-python 12.9.7` and passed `pip check`.
node14 provides Python 3.12 but no Python 3.11 executable; Python 3.11 wheel
coverage is therefore retained from the macOS installed-wheel gate rather than
reported as a node14 pass. See `node14-python-availability.log`.

## Acceptance Evidence

- AC-3-1: require-only collects all missing keys and preserves logical snapshot
  bytes in `test_require_only_reports_all_missing_without_logical_mutation`.
- AC-3-2: `test_real_b200_jit_publish_freeze_and_exact_cache_hit` runs the final
  installed wheel through NVRTC, CUDA execution, correctness, summarization,
  SQLite publication, freeze, and runner-free exact replay.
- AC-3-3: latency and II share one measurement ID while selecting their own
  aggregates in resolver tests and the real B200 workflow.
- AC-3-4: `_time_launch()` prepares kernel arguments before the start event;
  compilation, allocation, initialization, warmup, reset, first use, and output
  copies remain outside the event interval. The real runner test is retained in
  `node14-real-workflow.log`.
- AC-3-5: hash, compilation/execution, stability, correctness, and transaction
  failures publish no usable membership; corruption and rollback regressions
  are retained in the profile store/resolver tests.
- AC-3-6: deterministic frozen export/import, raw samples, environment,
  provenance, membership, atomic export, and CLI round trip are covered by
  `test_profile_store.py` and `test_profiles_cli.py`.
- AC-3-7: the installed-wheel root adds no CUDA backend modules, and a frozen
  exact hit resolves with `runner=None`; evidence is present in host wheel logs
  and the real frozen-cache workflow.

## Synchronization

The interrupted two-way session initially exposed stale beta conflicts. The
remote mirror was backed up, compared, and replaced with a scoped one-way
`costmodel` replica. Generated bytecode is excluded and was removed before the
final GPU run. The active session is connected with no conflicts. See
`sync-resolution.log` and `mutagen-sync.log`.

## Test Review

New tests close observable reachability gaps: exact SQLite corruption, atomic
export failure, special-character read-only paths, frozen-base creation,
provider support bounds, schema condition parity, real JIT publication, and the
real profile CLI cache hit. The lazy-import test was corrected to compare CUDA
modules before and after root import because `cuda-pathfinder` may pre-create
the top-level namespace. No source-shape or hypothetical-refactor guard was
added.
